from typing import List

def coin_change(coins: List[int], amount: int) -> int:
    """
    Determines the minimum number of coins required to make up a given amount.
    Returns -1 if it's not possible to form the amount with the given coins.

    Args:
        coins: List of coin denominations.
        amount: The total amount to form.

    Returns:
        Minimum number of coins needed, or -1 if the amount cannot be formed.
    """
    # TODO: Implement the coin change algorithm
    pass 