export class Trie {
  constructor() {
    // TODO: Initialize your data structure.
  }

  insert(word: string): void {
    // TODO: Insert the word into the trie.
  }

  search(word: string): boolean {
    // TODO: Return true if the word is in the trie, false otherwise.
    return false;
  }

  startsWith(prefix: string): boolean {
    // TODO: Return true if there is any word in the trie that starts with the given prefix.
    return false;
  }

  countWordsEqualTo(word: string): number {
    // TODO: Return the count of the exact word in the trie.
    return 0;
  }

  countWordsStartingWith(prefix: string): number {
    // TODO: Return the count of words in the trie that start with the given prefix.
    return 0;
  }

  erase(word: string): void {
    // TODO: Remove one occurrence of the word from the trie.
  }
}
