interface Activity {
  start: number;
  end: number;
}

/**
 * @param {Activity[]} activities - An array of activities with start and end times
 * @returns {Activity[]} The maximum set of non-overlapping activities
 */
export function activitySelection(activities: Activity[]): Activity[] {
  // TODO: Implement the activity selection algorithm
  return [];
}
