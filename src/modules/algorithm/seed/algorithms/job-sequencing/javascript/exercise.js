/**
 * @param {Job[]} jobs - An array of jobs with their profit and deadline
 * @returns {string[]} The sequence of job IDs that maximizes profit
 */
function jobSequencing(jobs) {
  // TODO: Implement the job sequencing algorithm
  return [];
}

module.exports = { jobSequencing };
