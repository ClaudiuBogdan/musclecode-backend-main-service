from typing import List

def articulation_points(graph: List[List[int]]) -> List[int]:
    """
    Finds the articulation points in an undirected graph.
    
    The graph is represented as an adjacency list where graph[i] is a list
    of neighbors of vertex i.
    
    Args:
        graph: A list of lists of integers representing the graph.
        
    Returns:
        A list of articulation points.
    """
    # TODO: Implement the articulation points algorithm
    return [] 