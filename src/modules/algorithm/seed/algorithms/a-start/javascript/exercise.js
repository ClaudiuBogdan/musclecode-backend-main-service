/**
 * A* Search Algorithm
 *
 * Finds the shortest path from start to goal in a grid using the A* search algorithm.
 * Cells with value 0 are walkable and cells with value 1 are obstacles.
 *
 * @param {number[][]} grid - A 2D grid representing the map.
 * @param {number[]} start - Starting coordinates [x, y].
 * @param {number[]} goal - Goal coordinates [x, y].
 * @returns {number[][]} The optimal path from start to goal, or an empty array if no path exists.
 */
function aStarSearch(grid, start, goal) {
  // TODO: Implement the A* search algorithm
  return [];
}

module.exports = { aStarSearch };
