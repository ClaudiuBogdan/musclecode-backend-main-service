/**
 * Finds the shortest path from start to goal in a grid using the A* search algorithm.
 * Cells with value 0 are walkable, and cells with value 1 are obstacles.
 *
 * @param grid - A 2D grid of numbers (0 for free, 1 for obstacle)
 * @param start - Starting coordinates as [x, y]
 * @param goal - Goal coordinates as [x, y]
 * @returns An array of coordinates representing the path from start to goal,
 *          or an empty array if no path exists.
 */
export function aStarSearch(
  grid: number[][],
  start: number[],
  goal: number[],
): number[][] {
  // TODO: Implement the A* search algorithm
  return [];
}
